{
	"name": "dxdevcheck",
	"displayName": "dxdevcheck",
	"description": "checks dev things",
	"version": "0.0.1",
	"publisher": "trebleCode",
	"engines": {
		"vscode": "^1.28.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onCommand:extension.runDevChecker"
	],
	"main": "./out/extension.js",
	"contributes": {
		"commands": [
			{
				"command": "extension.runDevChecker",
				"title": "DX DevCheck: Run Config Checker"
			}
		]
	},
	"scripts": {
		"vscode:prepublish": "vscode:prepublish: tsc -p ./",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"postinstall": "node ./node_modules/vscode/bin/install",
		"test": "npm run compile && node ./node_modules/vscode/bin/test"
	},
	"devDependencies": {
		"@types/mocha": "^2.2.32",
		"@types/node": "^11.9.4",
		"config": "^3.0.1",
		"configparser": "^0.2.6",
		"tslint": "^5.8.0",
		"typescript": "^2.9.2",
		"vscode": "^1.1.25"
	},
	"dependencies": {
		"graceful-fs": "^4.1.15",
		"ini": "^1.3.5",
		"replace-in-file": "^3.4.3"
	}
}
